[tool.ruff]
line-length = 120

target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "N"]
ignore = ["E501","I001","F401"]  
# E501: idk|I001:might be ignore unsorted imports | F401:to ignore unused imports


[tool.pytest.ini_options]
testpaths = ["engine/tests", "api/tests"]