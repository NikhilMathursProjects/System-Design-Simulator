[project]
name = "sysdesign-engine"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "simpy>=4.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"